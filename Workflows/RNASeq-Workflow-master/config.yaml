# Edit this file to adjust to your own workflow.

# Path to the data folder.
data: "data"

# Path to the file that describes the samples.
# This is a tab-separated file with two columns: sample name and condition.
# The first line is the header.
samples_info: "data/samples_info.tsv"

# Path to the transcriptome.
transcriptome: "data/arabidopsis.fa"

# Path to kallisto output that will be generated by the workflow.
kallisto_out: "data/kallisto_out"

# Path to sleuth output that will be generated by the workflow.
sleuth_out: "data/sleuth_out"

